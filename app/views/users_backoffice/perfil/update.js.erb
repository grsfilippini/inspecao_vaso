// Usando JQuery
$('#avatarModal').modal('hide');

// Usando JS
document.getElementById('imgAvatarLg').src = "<%= url_for(@user.perfil_usuario.avatar) %>"
document.getElementById('imgAvatarSm').src = "<%= url_for(@user.perfil_usuario.avatar) %>"
document.getElementById('AvatarModal').src = "<%= url_for(@user.perfil_usuario.avatar) %>"

$.bootstrapGrowl("Avatar atualizado com sucesso!", {
        ele: 'body', // which element to append to
        type: 'success', // (null, 'info', 'error', 'success')
        offset: {from: 'top', amount: 20}, // 'top', or 'bottom'
        align: 'right', // ('left', 'right', or 'center')
        width: 250, // (integer, or 'auto')
        delay: 4000,
        allow_dismiss: true,
        stackup_spacing: 10 // spacing between consecutively stacked growls.
    });