<div class="container-fluid">
    <!-- Page Heading -->
    <h1 class="h3 mb-4 text-gray-800"><%= t('mensagens.lista_de', model: @finalidade_do_vasos.model_name.human(count: @finalidade_do_vasos.count)) %></h1>
</div>

<div class="table-responsive">
    <table class="table table-striped table-bordered table-hover">    
        <thead>
            <tr>
                <th>#</th>                
                <th><%= traduz_atributos_humanos(@finalidade_do_vasos, :finalidade_do_vaso) %> </th>
                <th>
                    <%= link_to  new_admins_backoffice_finalidade_do_vaso_path, class:"btn btn-success btn-circle btn-sm" do %>
                        <i class="fas fa-plus"></i>
                    <% end %>
                </th>
            </tr>
        </thead>
        <tbody> 
            <% @finalidade_do_vasos.each do |f|%>                                                           
                <tr>
                    <td><%= f.id %></td>
                    <td><%= f.finalidade_do_vaso %></td>

                    <td style="width: 100px">
                        <%= link_to edit_admins_backoffice_finalidade_do_vaso_path(f.id), class:"btn btn-info btn-circle btn-sm" do %>
                            <i class="fas fa-edit"></i>
                        <%end%>
                        <%= link_to admins_backoffice_finalidade_do_vaso_path(f.id),
                                    method: :delete, class:"btn btn-danger btn-circle btn-sm", 
                                    data: { confirm: t('mensagens.confirmar_exclusao', item: f.finalidade_do_vaso)} do %>
                            <i class="fas fa-minus"></i> 
                        <%end%>
                    </td>
                </tr>
            <% end %>
        </tbody>
    </table>

    <%= paginate @finalidade_do_vasos %>
    
</div>