<div class="container-fluid">
    <!-- Page Heading -->
    <h1 class="h3 mb-4 text-gray-800"><%= t('mensagens.lista_de', model: @codigo_construcaos.model_name.human(count: @codigo_construcaos.count)) %> </h1>
</div>

<div class="table-responsive">
    <table class="table table-striped table-bordered table-hover">    
        <thead>
            <tr>
                <th>#</th>
                <th><%= traduz_atributos_humanos(@codigo_construcaos, :codigo) %> </th>                
                <th>
                    <%= link_to  new_admins_backoffice_codigo_construcao_path, class:"btn btn-success btn-circle btn-sm" do %>
                        <i class="fas fa-plus"></i>
                    <% end %>
                </th>
            </tr>
        </thead>
        <tbody> 
            <% @codigo_construcaos.each do |c|%>
                <tr>
                    <td><%= c.id %></td>
                    <td><%= c.codigo %></td>
                    <td style="width: 50px">
                        <%= link_to edit_admins_backoffice_codigo_construcao_path(c.id), class:"btn btn-info btn-circle btn-sm" do %> 
                            <i class="fas fa-edit"></i>
                        <%end%>
                    </td>
                </tr>
            <% end %>
        </tbody>
    </table>

    <%= paginate @codigo_construcaos %>
    
</div>