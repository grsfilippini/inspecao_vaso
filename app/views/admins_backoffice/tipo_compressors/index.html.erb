<div class="container-fluid">
    <!-- Page Heading -->
    <h1 class="h3 mb-4 text-gray-800"><%= t('mensagens.lista_de', model: @tipo_compressors.model_name.human(count: @tipo_compressors.count)) %> </h1>
</div>

<div class="table-responsive">
    <table class="table table-striped table-bordered table-hover">    
        <thead>
            <tr>
                <th>#</th>
                <th><%= traduz_atributos_humanos(@tipo_compressors, :tipo_compressors) %> </th>                
                <th>
                    <%= link_to  new_admins_backoffice_tipo_compressor_path, class:"btn btn-success btn-circle btn-sm" do %>
                        <i class="fas fa-plus"></i>
                    <% end %>
                </th>
            </tr>
        </thead>
        <tbody> 
            <% @tipo_compressors.each do |t|%>
                <tr>
                    <td><%= t.id %></td>
                    <td><%= t.tipo_compressor %></td>
                    
                    <td style="width: 50px">
                        <%= link_to edit_admins_backoffice_tipo_compressor_path(t.id), class:"btn btn-info btn-circle btn-sm" do %> 
                            <i class="fas fa-edit"></i>
                        <%end%>
                    </td>
                </tr>
            <% end %>
        </tbody>
    </table>

    <%= paginate @tipo_compressors %>
    
</div>