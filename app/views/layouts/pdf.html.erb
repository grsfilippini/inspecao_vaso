<!DOCTYPE html>
<html>
<head>
    <title></title>
    <%= wicked_pdf_stylesheet_link_tag 'pdf' %>        
    <meta charset="UTF-8">

    <style>
        /* Estilo de página para relatórios */
        @page {
            size: A4;
            margin-top: 10mm;
            margin-bottom: 10mm;
            margin-left: 10mm;
            margin-right: 10mm;
            /*padding: 2mm;*/
        }

        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            /*font-size: 10pt; /* Tamanho da fonte */
            /*line-height: 14pt; /* Altura da linha (line-height) */
            line-height: 1.3; /* Espaçamento entre linhas de 1.5 */
        }

        .page {
            width: calc(210mm - 10mm - 10mm); /* Largura da página A4 menos margens */
            height: calc(297mm - 10mm - 10mm); /* Altura da página A4 menos margens */
            margin: auto;
            padding: 2mm;
            box-sizing: border-box;
            page-break-after: always;
            /*border: 1px solid black;*/ /* Adiciona uma borda */
        }

        @media print {
            body, .page {
                margin: 0;
                box-shadow: none;
                page-break-after: auto;
            }
        }

        /* Estilo de tabela para cabeçalho da empresa */
        .tableCabecalho {
            width: 233mm;
            border-collapse: collapse;
            margin-bottom: 20px; /* Espaço entre as tabelas */
            border: 1px solid black;
        }
        .tableCabecalho th, .tableCabecalho td {
            border: 1px solid black;
            height: 25mm; /* Definindo a altura da linha */
        }
        .tableCabecalho .col1 {
            width: 25mm; 
            text-align: center;
            vertical-align: middle;
        }
        .tableCabecalho .col2 {
            width: 168mm; /* 230-40-25 */
        }

        /* Estilo de tabela para Assinatura escrita e digital */
        .tableAssinatura {
            width: 233mm;
            border-collapse: collapse;
            margin-bottom: 20px; /* Espaço entre as tabelas */
            border: 1px solid black;
        }
        .tableAssinatura th, .tableAssinatura td {
            /*border: 1px solid black;*/
            /*border: none;*/ /* Remove as bordas das células */
            height: 25mm; /* Definindo a altura da linha */
        }
        .tableAssinatura .col1 {
            width: 80mm; 
        }
        .tableAssinatura .col2 {
            width: 80mm;
        }
        .tableAssinatura .col3 {
            width: 73mm;
        }
    </style>

</head>
<body>
    <%= yield %>
</body>
</html>

